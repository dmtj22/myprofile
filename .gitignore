<!DOCTYPE html>
<html>
<head>
		<meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta property="og:title" content="정은선 프로필">
    <meta property="og:description" content="연구간호사">
    <meta property="og:image" content="정은선AI프로필사진">
    <title>정은선 프로필</title>
    <style>
         @import url('https://cdn.jsdelivr.net/gh/orioncactus/pretendard/dist/web/static/pretendard.css');
        * {
        font-family: "Pretendard",serif;
}
        body{
            background-color: #44398A;
        }
        .profile{
            width: 100px;
            height: 100px;

            border-radius: 100%;

            border: 2px solid white;

            background-image: url'정은선AI프로필사진.jpg';
            background-position: center;
            background-size: cover;
        }

        .main{
            color:white;
            font-size: 20px;

            margin-top: 30px;
            margin-bottom: 0px;
        }
        .sub-1{
            color:white;
            font-size: 14px;

            margin-top: 20px;
            margin-bottom: 0px;
        }
	.sub-2{
            color:white;
            font-size: 14px;
